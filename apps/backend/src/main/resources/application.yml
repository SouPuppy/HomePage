server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/homepage
    username: homepage_admin
    password: Richy2025

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
